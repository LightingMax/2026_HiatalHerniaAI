# Windows CPU build dependencies (non-torch)
# Torch is installed separately with pinned version in build scripts:
#   torch==2.8.0 / torchvision==0.23.0 (CPU wheels)

numpy
pandas
Pillow
timm
openpyxl
PyQt5
pyinstaller
